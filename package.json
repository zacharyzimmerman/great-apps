{
  "name": "great-apps",
  "version": "1.0.0",
  "private": true,
  "description": "Shared JS/CSS framework + service-worker core for the Great Apps suite.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
